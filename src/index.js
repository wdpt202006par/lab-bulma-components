import React from 'react'; // On importe React 
import ReactDOM from 'react-dom'; // On importe React-dom
import App from './App'; // On importe notre fichier App

ReactDOM.render(<App />, document.getElementById('root')); // On importe notre fonction App dans l'endroit id 'root'
